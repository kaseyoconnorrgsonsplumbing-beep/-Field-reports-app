rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function companyUser() {
      // Accounts are created by hand in Firebase > Authentication > Users,
      // so any signed-in company address is trusted.
      return request.auth != null
        && request.auth.token.email.matches('.*@rgsonsplumbing[.]com$');
    }
    match /reports/{reportId} {
      allow read, write: if companyUser();
      // photos, renders, and anything else stored under a report
      match /{sub}/{docId} {
        allow read, write: if companyUser();
      }
    }
  }
}
